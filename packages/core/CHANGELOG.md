# Changelog

完整变更记录见项目仓库根目录的 CHANGELOG.md。

## Unreleased

- `showCheckbox` 只控制选择能力和选择控件，`multiple` 独立控制单选/多选。
- 增加节点整行选择/展开、自定义过滤、关键词高亮和空状态插槽。
- 增加 `scrollToKey`、懒加载错误事件和重试方法。
- 优化移动端触控反馈、禁用选中态和加载状态。
